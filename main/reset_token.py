from config_reads import config_reads

config_reads.edit_key(value="", section=config_reads.config_main_n, key=config_reads.token_ciphered_base64encoded_utf8_n)
config_reads.edit_key(value="", section=config_reads.config_main_n, key=config_reads.token_ciphered_iv_base64encoded_utf8_n)
config_reads.edit_key(value="", section=config_reads.config_main_n, key=config_reads.salt_base64encoded_utf8_n)
